
name: Publish Website to Web Hosting
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.measuresummit.com
        username: main@measuresummit.com
        password: ${{ secrets.ftp_password }}
        server-dir: /public_html/wp-content/themes/hello-elementor/
        port: 21
